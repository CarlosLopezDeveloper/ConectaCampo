ConectaCampo REST API

- Setup Spring Boot project structure with JPA, MySQL, Security, and Lombok
- Implement MVC architecture (Model, Repository, Service, Controller)
- Add CRUD endpoints for Products, Categories, Administrators, and Contact Enquiries
- Configure JPA entities, relationships, and custom search filters
- Include SQL scripts for database initialization and initial data seeding
